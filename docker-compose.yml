services:
  # Primary Database Engine: PostgreSQL
  postgres:
    image: postgres:16-alpine
    container_name: file4base-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: file4base_dev
      POSTGRES_USER: file4base
      POSTGRES_PASSWORD: dev_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U file4base -d file4base_dev"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - file4base-net

  # Alternative Engine Profile: MariaDB (start with: docker compose --profile mariadb up)
  mariadb:
    image: mariadb:11-jammy
    container_name: file4base-mariadb
    restart: unless-stopped
    profiles: ["mariadb"]
    environment:
      MARIADB_DATABASE: file4base_dev
      MARIADB_USER: file4base
      MARIADB_PASSWORD: dev_password
      MARIADB_ROOT_PASSWORD: root_password
    ports:
      - "3306:3306"
    volumes:
      - mariadb_data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "healthcheck.sh --connect --innodb_initialized"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - file4base-net

  # Go Backend Core API Service
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: file4base-server
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      DB_ENGINE: "postgres"
      DATABASE_URL: "postgres://file4base:dev_password@postgres:5432/file4base_dev?sslmode=disable"
      SERVER_PORT: ":8080"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080/healthz/readiness || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 5s
    networks:
      - file4base-net

  # WebDirect Client (Web UI served when launched as a Server via docker compose)
  web:
    build:
      context: ./client
      dockerfile: Dockerfile
    container_name: file4base-web
    restart: unless-stopped
    ports:
      - "3000:80"
    depends_on:
      server:
        condition: service_healthy
    networks:
      - file4base-net

volumes:
  postgres_data:
  mariadb_data:

networks:
  file4base-net:
    driver: bridge
